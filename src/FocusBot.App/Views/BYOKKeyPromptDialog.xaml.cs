using Microsoft.UI.Xaml.Controls;

namespace FocusBot.App.Views;

public sealed partial class BYOKKeyPromptDialog : ContentDialog
{
    public BYOKKeyPromptDialog()
    {
        InitializeComponent();
    }
}
